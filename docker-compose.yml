version: '3'
services:
  jupyter:
    build: .
    volumes:
      - .:/home/jovyan/work
    ports:
      - 8888:8888
    container_name: algorithms4pepprotgraphs
    command: "start-notebook.sh --NotebookApp.token="